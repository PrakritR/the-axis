# Copy this file to .env and fill in your values
# WARNING: All VITE_ variables are bundled into the client-side build and visible to users.
# For production, proxy API requests through a backend instead.

# Set to true (or 1/yes) to show a full-site maintenance page for every route. Omit or set false to serve the normal app.
# VITE_MAINTENANCE_MODE=true

# Google Gemini API key — get from https://aistudio.google.com
# Create a key in a NEW project to get a fresh free-tier quota
VITE_GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic — server-side lease generation for the manager portal
# Add this to your local `.env` and to Vercel project environment variables.
# Do not prefix with VITE_ or it will be exposed to the browser bundle.
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# /admin — site owner sign-in (server-only; never use VITE_*). Required for owner login at /admin.
# Local: ensure the API dev server proxies /api (see vite.config.mjs LOCAL_API_PORT).
# SITE_OWNER_EMAIL=you@example.com
# SITE_OWNER_PASSWORD=use-a-strong-secret

# /admin — CEO login (email + password, full internal access). Server-only.
# Defaults match local setup; set all three in Vercel for production.
# AXIS_CEO_EMAIL=prakritramachandran@gmail.com
# AXIS_CEO_PASSWORD=Welcone56$
# AXIS_CEO_NAME=Prakrit

# /admin — Admin Profile table (same base as VITE_AIRTABLE_BASE_ID). Email + Password + Role (CEO, CTO, CFO, SWE, Admin).
# Optional override if your table is not named "Admin Profile".
# AIRTABLE_ADMIN_PROFILE_TABLE=Admin Profile

# EmailJS credentials — get from https://www.emailjs.com
# 1. Add a Gmail service → copy Service ID
# 2. Create an email template → copy Template ID
# 3. Account → API Keys → copy Public Key
VITE_EMAILJS_SERVICE_ID=your_emailjs_service_id
VITE_EMAILJS_TEMPLATE_ID=your_emailjs_template_id
VITE_EMAILJS_PUBLIC_KEY=your_emailjs_public_key
# Optional dedicated template for lease signing invites sent by `/api/send-lease-invite`
VITE_EMAILJS_LEASE_TEMPLATE=your_emailjs_lease_template_id
# Template for portal message notifications (tells recipient to check the portal inbox)
# Template variables: to_email, to_name, sender_name, subject, portal_url
VITE_EMAILJS_MESSAGE_NOTIFY_TEMPLATE=your_emailjs_message_notify_template_id

# Supabase — Resident Portal auth and photo storage
# Get from: Supabase project → Settings → API
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
# Supabase Storage bucket name for resident photos (default: resident-uploads)
VITE_SUPABASE_STORAGE_BUCKET=resident-uploads

# Airtable — Shared API token (single base: VITE_AIRTABLE_BASE_ID)
# Get from: airtable.com → Account → Developer Hub → Personal access tokens
# Scopes needed: data.records:read, data.records:write on your Airtable base
VITE_AIRTABLE_TOKEN=your_airtable_personal_access_token
# Server-only duplicate (recommended on Vercel): API routes may not see VITE_* at runtime.
# AIRTABLE_TOKEN=your_airtable_personal_access_token

# Airtable — single base for the whole app (portal, Apply, tour/Scheduling, lease drafts, SignForge, etc.)
VITE_AIRTABLE_BASE_ID=appol57LKtMKaQ75T

# Work Orders — if Airtable rejects linking the portal resident (e.g. wrong table / sync), set this to a Resident Profile
# record id that always works as the link target (often a blank-name row shown as "Unnamed record").
# The portal still lists each resident's orders using an internal description tag and/or optional email field below.
# VITE_AIRTABLE_WORK_ORDER_RESIDENT_RECORD_ID=recXXXXXXXXXXXXXX
# Optional: single-line text field on Work Orders for the real submitter email when using the placeholder resident:
# VITE_AIRTABLE_WORK_ORDER_SUBMITTER_EMAIL_FIELD=Submitter Email

# Optional Rooms table (linked to Properties) — admin portal uses it to show minimum rent per property when present.
# VITE_AIRTABLE_ROOMS_TABLE=Rooms
# VITE_AIRTABLE_ROOM_RENT_FIELD=Monthly Rent
# Resident portal → Axis admin thread: inbox routes messages to this email (shown as "Axis admin" in UI). Client-visible VITE_ var.
# VITE_PORTAL_AXIS_ADMIN_EMAIL=ops@yourcompany.com

# Portal message notifications — email sent to all ENABLED admins in the Admin Profile table.
# Add an "Enabled" checkbox field to Admin Profile in Airtable; only checked admins receive notifications.
# Field name is configurable if you use a different name (default: Enabled).
# AIRTABLE_ADMIN_ENABLED_FIELD=Enabled

# Portal inbox (Management ↔ Admin, Site manager ↔ Admin) — Messages table (internal threads; work orders do not use it).
# 1) Add text field "Thread Key" and single select "Channel" with option internal_mgmt_admin (or override names below).
# 2) Allow Work Order to be empty on Messages rows created from these threads.
# 3) Optional: create an Airtable Form on Messages and paste its share URL here (prefill_* will be appended in-app).
# VITE_AIRTABLE_MESSAGE_THREAD_KEY_FIELD=Thread Key
# VITE_AIRTABLE_MESSAGE_CHANNEL_FIELD=Channel
# Optional single-line text on Messages for subject lines (inbox UI + sends). Set to "none" to disable.
# VITE_AIRTABLE_MESSAGE_SUBJECT_FIELD=Subject
# VITE_AIRTABLE_PORTAL_INBOX_FORM_URL=https://airtable.com/appXXXX/shrXXXX

# Manager inbox folders (Unopened / Opened / Trash) — optional table in the same base as Messages.
# Default table name: Inbox Thread State. Set to "none" to use browser localStorage only (not synced).
# VITE_AIRTABLE_INBOX_THREAD_STATE_TABLE=Inbox Thread State

# Apply flow — table names in the same base as VITE_AIRTABLE_BASE_ID
VITE_AIRTABLE_APPLICATIONS_TABLE=Applications
# Optional: checkbox field name for rejected applications (default Rejected). Required for Rejected tab/counts — Airtable omits unchecked checkboxes in API responses.
# VITE_AIRTABLE_APPLICATION_REJECTED_FIELD=Rejected
# AIRTABLE_APPLICATION_REJECTED_FIELD=Rejected
# Server-only mirror (Vercel) if you do not expose VITE_* to Functions:
# AIRTABLE_APPLICATIONS_TABLE=Applications
VITE_AIRTABLE_COAPPLICANTS_TABLE=Co-Signers

# Stripe
# Publishable key is safe for the client. Secret key must stay server-side only.
VITE_STRIPE_PUBLISHABLE_KEY=pk_test_your_stripe_publishable_key
STRIPE_SECRET_KEY=sk_test_your_stripe_secret_key
# Recurring subscription Price ID used for manager portal onboarding.
# Create a monthly recurring price in Stripe, then paste the price id here.
STRIPE_MANAGER_PRICE_ID=price_your_manager_monthly_price_id
# Optional: store each resident's Stripe customer ID in Airtable field:
# `Stripe Customer ID`

# Production: set for canonical URLs / sitemap. Local dev defaults to http://localhost:5174.
# VITE_SITE_URL=https://your-live-domain.com
# Optional — only for getHousingSiteOrigin() when window is unavailable (e.g. absolute links in emails). Nav uses same-origin paths.
# VITE_HOUSING_SITE_URL=https://your-live-domain.com
# Optional housing home path for in-app <Link> (default /). Full https URLs are reduced to pathname only so Explore properties stays on the current origin in dev.
# VITE_HOUSING_EXPLORE_PATH=/
