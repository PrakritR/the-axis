/**
 * Local dev entrypoint for `scripts/local-api-server.mjs`.
 * Canonical implementation lives in repo-root `api/[route].js` (Vercel serverless entry).
 */
export { default } from '../../api/[route].js'
